"use client";

// This component is no longer used — replaced by the code editor visual in hero-visual.tsx
// Kept as a placeholder to avoid import errors from any stale references.

export function InnovationChart() {
  return null;
}
